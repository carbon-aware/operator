apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbonaware.fullname" . }}
  labels:
    {{- include "carbonaware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "carbonaware.selectorLabels" . | nindent 4 }}
